#ifndef PREFS_H
#define PREFS_H

// DEBUG: warnings are on, extra checks may be in place
// PARTIAL_DEBUG: gives some warnings about checks but doesn't do any extra logic
// RELEASE: no warnings, no excessive safeguards or checks

#define DEBUG

#endif